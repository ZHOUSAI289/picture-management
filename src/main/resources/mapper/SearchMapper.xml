<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.gzlg.mapper.SearchMapper">

    <resultMap id="imageMap" type="com.gzlg.pojo.entity.image">
        <id property="id" column="id"/>
        <result property="title" column="title"/>
        <result property="category" column="category"/>
        <result property="photographer" column="photographer"/>
        <result property="personName" column="person_name"/>
        <result property="date" column="date"/>
        <result property="url" column="url"/>
        <result property="description" column="description"/>
        <result property="status" column="status"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <select id="query" resultMap="imageMap">
        select * from image
        <where>
            <if test="personName != null and personName != ''">
                and person_name like concat('%',#{personName},'%')
            </if>
        </where>
        order by created_at desc
    </select>
</mapper>
